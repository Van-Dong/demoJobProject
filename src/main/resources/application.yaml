server:
  servlet:
    context-path: /
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/deep_sleeping
    username: root
    password: dongnv
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  